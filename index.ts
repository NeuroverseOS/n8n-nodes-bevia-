// n8n-nodes-bevia — package entry point.
//
// n8n loads nodes and credentials from the paths declared in the
// `n8n` block in package.json. This file exists so the package has
// a stable `main` for tooling that expects one (eslint, ts-node,
// IDE indexers). It deliberately re-exports nothing at runtime.

export {};
